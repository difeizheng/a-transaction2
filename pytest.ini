[pytest]
# 把项目根加入 sys.path，使 `from src...` 在任意 cwd 下都能解析
pythonpath = .
testpaths = tests
# 未注册的 marker 视为错误，防拼写错误静默通过（--strict-markers）
markers =
    unit: 纯函数 / 无外部依赖（无网络、无 LLM、无真实库）的单元测试
    integration: 需要 SQLite 临时库或临时文件 IO 的集成测试
# -ra 汇总所有非通过结果；-q 安静输出
addopts = -ra --strict-markers
